[project]
name = "legal-auto-motion-harness"
version = "0.1.0"
description = "Fact-safe SRT + narration to isolated Remotion scene workflow"
requires-python = ">=3.11"

[project.scripts]
legal-motion = "legal_auto_motion.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

